<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>disable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <RootNamespace>ShadowsMcp.TestHost</RootNamespace>
    <AssemblyName>ShadowsMcp.TestHost</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- Same sources as the mod's protocol layer: what we smoke-test here is what ships. -->
    <Compile Include="../Core/**/*.cs" LinkBase="Core" />
  </ItemGroup>

</Project>
